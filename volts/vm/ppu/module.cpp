#include "module.h"

#include "vm/vm.h"

namespace volts::ppu
{
    void load_module(loader::elf::ppu_prx& mod)
    {
        for(auto& prog : mod.progs)
        {

        }

        for(auto& sect : mod.sects)
        {

        }
    }
}